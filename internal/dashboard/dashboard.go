// Package dashboard serves the HTMX server-rendered UI and SSE live-update
// endpoints (processing feed, queue stats, error panel, metrics), behind session auth.
package dashboard

// TODO(phase-06): HTML handlers + SSE endpoints; templates in web/.
