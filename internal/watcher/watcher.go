// Package watcher contains the data plane: per-collection change-stream
// watchers and the Manager that reconciles them against desired state.
package watcher

// TODO(phase-03): Watcher (Watch + updateLookup + resume tokens) and Manager (reconcile).
