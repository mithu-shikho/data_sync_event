// Package publisher defines the queue-agnostic Publisher interface and its
// implementations (NATS JetStream first). Swapping queues = a new impl here only.
package publisher

// TODO(phase-02): Publisher interface, ChangeEvent, partition helper, nats_jetstream.go.
