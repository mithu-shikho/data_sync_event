// Package api exposes the JSON REST API (/api/v1/...) for CRUD on connections
// and watches, gated by JWT bearer auth.
package api

// TODO(phase-06): REST handlers behind JWT; enable/disable triggers manager reconcile.
