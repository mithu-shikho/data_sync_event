// Package metrics is the in-process observability hub: counters, a bounded
// recent-event ring buffer, an SSE fan-out hub, and a JetStream stats poller.
package metrics

// TODO(phase-04): counters, sliding-window rate, ring buffer, SSE hub, JetStream poller.
