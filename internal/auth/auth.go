// Package auth provides Google OIDC dashboard login (session cookie) and
// JWT bearer authentication for the API.
package auth

// TODO(phase-05): OIDC login, session cookie, allowlist, JWT issue/verify middleware.
